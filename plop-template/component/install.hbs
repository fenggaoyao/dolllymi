import Do{{properCase name}} from './src/index.vue'

const components = [
Do{{properCase name}}
]

const install = (app) => {
components.forEach(component => {
app.component(component.name, component)
})
}

export {
Do{{properCase name}},
install
}
